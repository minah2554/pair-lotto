# 🎰 PAIR LOTTO (짝꿍 로또) 사용 설명서

본 문서는 **PAIR LOTTO(시험기간 점수 예측 활동)** 웹앱의 선생님(관리자) 및 학생용 전체 사용 매뉴얼입니다.

---

## 📌 목차
1. [교사용 (관리자 모드) 안내](#1-교사용-관리자-모드-안내)
   - 관리자 모드 진입 방법
   - 학생 비밀번호 분실 시 재설정 방법 (웹앱 & 구글 시트)
   - 구글 시트 '0000' 인식 및 보존 방법
   - 학급 인원 홀수 대응 (1인당 최대 2페어 지원)
   - 응모 마감(Lock) 및 성적 업로드/결과 발표
2. [학생용 안내](#2-학생용-안내)
   - 첫 접속 및 초기 비밀번호 설정
   - 친구와 페어(PAIR) 맺기 및 1인 2페어 규칙
   - 시험 퀘스트(미션) 인증 및 당첨 범위 보너스
   - 시험 후 로또 결과 확인

---

## 1. 교사용 (관리자 모드) 안내

### ① 관리자 모드 진입 방법 (시크릿 진입)
학생들에게 관리자 모드가 노출되지 않도록 버튼을 숨겨두었습니다.
- 첫 화면(로그인 화면) 상단의 **`🎰 PAIR LOTTO` 글자(타이틀)**를 클릭(터치)합니다.
- 관리자 로그인 창이 나타나면 비밀번호 **`minah`**를 입력하고 로그인합니다.
*(※ 비밀번호는 브라우저 개발자 도구에 노출되지 않도록 SHA-256 해시로 안전하게 암호화되어 검증됩니다.)*

### ② 학생 비밀번호 분실 시 재설정 방법
학생이 비밀번호를 잊어버린 경우, 선생님께서 **웹앱** 또는 **구글 스프레드시트**에서 즉시 변경하실 수 있습니다.

#### 방법 A: 웹앱 관리자 화면에서 변경 (추천)
1. 관리자 모드로 로그인합니다.
2. 상단 탭에서 **[👥 학생 관리]**로 이동합니다.
3. 해당 학생 이름 옆의 **`[🔑 PIN 변경]`** 버튼을 누릅니다.
4. 새로 설정할 4자리 숫자(예: `1234` 또는 `0000`)를 입력하고 확인을 누르면 **즉시 변경**되어 학생이 바로 로그인할 수 있습니다.

#### 방법 B: 구글 스프레드시트에서 직접 변경
1. 연동된 Google Sheets의 **`STUDENTS`** 시트를 엽니다.
2. 5번째 열(`pinHash` 또는 `pin`)에서 해당 학생 행의 셀을 원하는 4자리 숫자로 입력합니다.
3. 수정 즉시 웹앱에 반영됩니다.

---

### ③ 구글 시트에서 '0000' 등 앞자리 0 인식 방법
구글 시트는 숫자로 입력 시 `0000`을 숫자 `0`으로 자동 축소하는 특성이 있습니다. 이를 방지하고 100% 인식하도록 개선되었습니다:
1. **시트에서 직접 입력할 때**: 숫자 앞에 작은따옴표(`'`)를 붙여 **`'0000`** 또는 **`'0123`** 형태로 입력하시면 텍스트로 안전하게 저장됩니다.
2. **자동 보정 기능 탑재**: 시트에 숫자 `0`으로 들어가 있더라도, 웹앱 백엔드 코드가 자동으로 `0000`으로 복원하여 학생 로그인을 정상 처리합니다.

---

### ④ 학급 인원 홀수 대응 (1인당 최대 2개 페어 허용)
학급 인원이 홀수(예: 25명, 27명, 29명)인 경우 1인 1페어로 제한하면 1명의 학생이 짝꿍 없이 남는 문제가 발생합니다.
- **해결 규칙**: 학생 1명이 **최대 2개의 페어(2명의 친구와 매칭)**까지 참여할 수 있습니다.
- 남는 학생이 있는 경우, 이미 페어를 맺은 친구 중 1명이 해당 학생의 2번째 페어로 추가 매칭을 맺어주면 학급 전원이 소외 없이 참여할 수 있습니다.
- 단, 동일한 두 학생이 같은 과목에서 중복으로 2번 맺는 것은 자동으로 방지됩니다.

---

### ⑤ 응모 마감(Lock) 및 결과 발표
1. **응모 마감(잠금)**: 관리자 대시보드 상단의 `[🚨 전체 응모 마감하기]`를 누르면 학생들이 더 이상 페어를 변경하거나 신청할 수 없도록 수정이 잠깁니다.
2. **성적 업로드**: 시험이 끝난 후 [시험 결과 업로드] 탭에서 학생별 과목 점수 CSV를 업로드합니다.
3. **결과 판정**: `[🏆 결과 판정 실행]` 버튼을 누르면 두 학생의 합산 점수와 TARGET 점수를 대조하여 JACKPOT, WIN, NEAR MISS, MISS가 자동 계산됩니다.

---

## 2. 학생용 안내

### ① 첫 접속 및 초기 비밀번호 설정
1. 첫 화면에서 **[처음이에요 (초기 비밀번호 설정)]** 버튼을 누릅니다.
2. 본인의 **학번 4자리**(예: `2201`)와 **이름**, 그리고 앞으로 사용할 **초기 비밀번호 4자리**를 입력합니다.
3. **⚠️ 주의사항**: 설정한 초기 비밀번호는 앞으로 로그인할 때 계속 사용되므로 **반드시 기억**하고 있어야 합니다! (비밀번호를 잊어버린 경우 선생님께 초기화를 요청하세요.)

---

### ② 친구와 페어(PAIR) 맺기
1. 로그인 후 화면 중앙의 **[🎯 SELECT YOUR PAIR]**로 이동합니다.
2. 도전할 **과목**, 함께할 **친구**, 둘의 예상 합산 점수인 **TARGET 점수**(예: 180점)를 선택합니다.
3. **`[ 🚀 SEND PAIR REQUEST ]`**를 누르면 친구에게 매칭 신청이 전송됩니다.
4. 상대방 친구가 화면 상단의 `⚡ INCOMING PAIR REQUESTS`에서 **`[ ✅ 수락 ]`**을 누르면 **`🎉 PAIR MATCH!`** 축하 연출과 함께 디지털 로또 티켓이 발급됩니다.
5. **1인당 최대 2페어**: 학급 인원이 홀수인 경우를 위해 1인당 최대 2명의 친구와 페어를 맺을 수 있습니다.

---

### ③ 시험 퀘스트(미션) 인증 & 버프 획득
1. 페어가 맺어지면 **[⭐ QUEST BOARD]**가 열립니다.
2. 시험 전 미션(예상 문제 공유, 오답 정리 도와주기 등)을 수행합니다.
3. **`[ 📸 PHOTO VERIFY ]`** 버튼을 눌러 공부한 사진을 인증합니다.
4. 미션을 완료할 때마다 나의 로또 티켓 당첨 오차 범위(BONUS)가 **`±5점 → ±6점 → ±7점`**으로 넓어져 당첨 확률이 대폭 상승합니다!

---

### ④ 시험 후 로또 결과 확인
1. 시험이 끝나고 성적이 발표되면 화면 하단 [🏁 LOTTO REVEAL]에 결과 확인 버튼이 활성화됩니다.
2. **`[ 🎰 CHECK RESULT ]`** 버튼을 누르면 긴장감 넘치는 슬롯머신 숫자 롤링 연출이 시작됩니다.
3. 두 사람의 시험 점수 합계가 계산되고 결과가 공개됩니다:
   - **🎊 JACKPOT**: 목표 TARGET 점수를 1의 자리까지 정확하게 맞춘 경우!
   - **🎉 WIN**: 미션 보너스로 넓어진 최종 당첨 범위 안에 들어온 경우!
   - **😮 NEAR MISS**: 아쉽게 3점 이내 오차로 빗겨간 경우!
   - **💫 MISS**: 목표 범위를 벗어난 경우 (다음 시험에 다시 도전!)

---
*제작 및 권리: MINARI STUDIO · Pair Lotto Web App v1.2*
